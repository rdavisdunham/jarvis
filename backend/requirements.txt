groq
pyinotify
requests

# Required for local streaming STT (STT_PROVIDER=local)
faster-whisper
websockets>=12.0

# Required only if TTS_PROVIDER=kokoro (local Kokoro-82M on GPU)
numpy
soundfile
kokoro>=0.9.4

# End-of-utterance detection (optional, EOU_ENABLED=true)
# Uses LiveKit's ONNX model directly via onnxruntime + transformers (already installed as transitive deps)
# No additional packages needed — onnxruntime, transformers, huggingface_hub come from other deps

# Long-term memory (Mem0 + Qdrant)
mem0ai
qdrant-client
sentence-transformers
